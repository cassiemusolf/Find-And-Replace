using System;
using System.Collections.Generic;

namespace FindAndReplace.Objects

{
  public class Find
  {
    public string _originalString;
    public string _wordToReplace;
    public string _replaceWith;

    public string FindAndReplace(string originalString, string wordToReplace, string replaceWith)
    {
      // _originalString = originalString;
      // _wordToReplace = wordtoReplace;
      // _replaceWith = replaceWith;


        return replaceWith + " world";

    }
  }
}
